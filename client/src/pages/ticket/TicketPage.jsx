import React from "react";
import SeatSelection from "../Seats/Seat";
const TicketPage = (props) => {
  return (
    <>
      <SeatSelection id={props.id} />

    </>
  );
};

export default TicketPage;
